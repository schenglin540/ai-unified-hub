# OpenAPI Playground — 第二阶段视觉设计基线

## 三个方向候选

### 方向 A：精密工作台

**Very Brief Intro：** 以实验仪器、版本控制与代码编辑器为灵感，采用深墨与骨白两套高对比主题，构建可长时间使用的严谨工作界面。强调清晰分区、信息层级与可信赖的状态反馈。  
**Probability：** 0.07

### 方向 B：暖灰技术档案

**Very Brief Intro：** 用纸张般的暖灰底、墨黑文字与钴蓝标记营造“工程记录本”的克制感，突出请求历史与可审计性。整体更接近技术出版物而非传统 SaaS。
**Probability：** 0.03

### 方向 C：信号矩阵

**Very Brief Intro：** 通过高密度网格、低饱和状态色和微弱扫描线，表现多模型请求的并行与数据流。整体偏向系统监控台，但避免霓虹和赛博朋克视觉套路。
**Probability：** 0.09

---

## 已选方向：精密工作台

### Design Movement

现代开发者工作台，融合 GitHub 的可读性、Linear 的空间秩序、桌面 IDE 的局部密度与实验仪器的状态可信度。不是营销页面，也不是聊天产品；每一个视觉区域都应服务于“构造、发送、检查、理解 API 请求”。

### Core Principles

1. **边界即导航：** 使用细腻边框、分栏、固定上下文栏和局部面板说明功能关系，避免悬浮卡片堆叠。
2. **信息分层：** 请求状态、模型与响应内容优先；原始 JSON、代码与详细诊断通过标签页或抽屉渐进显示。
3. **高密度而不拥挤：** 以 4px 间距节奏、紧凑行高和等宽数据列组织复杂信息，并保留关键留白。
4. **可验证反馈：** 成功、失败、流式与中止状态同时使用图标、文字和颜色；所有交互反馈即时、克制、可追踪。

### Color Philosophy

以近中性石墨、暖白和低饱和蓝绿为基础。蓝绿色 `#0F766E` 是唯一的品牌主色，用于“可执行、可连接、可确认”的积极操作；红、琥珀与天蓝仅表达状态，不承担品牌装饰。深色主题采用墨黑与蓝灰，而不是纯黑和荧光色，以支持长时间阅读代码与 JSON。

### Layout Paradigm

采用 **固定侧轨 + 上下文栏 + 可伸缩工作画布**。左侧是长期导航与隐私状态，主区由页面自身定义任务栅格。Playground 在宽屏保持“配置—编辑—检查”三条竖向轨道；中等尺寸折叠为“配置/编辑 + 检查”两轨；移动端按执行顺序垂直叠放。

### Signature Elements

1. **状态轨迹线：** 请求状态与时间线使用细线、节点和短文字，重复出现于 Playground、History 和 Compare。
2. **精密角标：** 面板标题左侧配置窄色条或编号式短标签，帮助扫描复杂工作区。
3. **网格纸细纹：** 仅在空状态和 Dashboard 背景上使用极低对比度坐标网格，表达“实验台”概念。

### Interaction Philosophy

交互优先减少认知切换。发送、停止、重试等高频操作就在任务附近；状态更新不可闪烁；危险操作需明确确认。键盘命令即时打开，鼠标悬停仅用于补充信息，不隐藏核心功能。

### Animation

微交互以 120–220ms 的透明度与位移过渡为主，使用 `cubic-bezier(0.23, 1, 0.32, 1)`。流式输出使用光标与节流增量，非数据本身的装饰动画保持极少。抽屉和对话框从触发方向轻微滑入；尊重 `prefers-reduced-motion`。

### Typography System

UI 文本使用 `IBM Plex Sans`，标题使用同家族较重字重，代码、模型名、指标与 JSON 使用 `JetBrains Mono`。页面标题 24–28px，面板标题 12–13px 全大写或宽字距，正文 13–14px；避免 Inter 作为全站默认字体。

### Brand Essence

**OpenAPI Playground 是为多 Provider AI API 开发者准备的、本地优先的可观测调试工作台。**  
人格：**严谨、克制、透明**。

### Brand Voice

标题、CTA 与微文案应直接说明下一步操作和结果，不使用营销承诺。

示例：

> “构造一次请求，检查每一处信号。”

> “尚未配置 Provider。先添加一个连接，再开始测试。”

### Wordmark & Logo

标志为一个由两条开口括号与中心脉冲点构成的抽象 API 请求符号：外层表示请求与响应边界，中心点表示可观察的执行状态。图形不包含文字，适合 24–32px 的侧边栏与 favicon 尺寸；文字标使用紧凑的 IBM Plex Sans Medium 组合。

### Signature Brand Color

**Precision Teal — `#0F766E`**。

## Style Decisions

- 所有页面保持工具型工作台结构，不出现 Hero、营销插图或大幅渐变。
- 主题切换覆盖 light / dark / system，主题逻辑统一放在全局 token。
- API Key 的掩码、Local-first 安全边界和 Demo Mode 标识属于关键 UI，不能以装饰元素替代。
- Dashboard 与 History 不展示未经明确标识的假请求统计；示例数据必须带有 `Demo data` 标记。
- 核心工作台在没有 Provider 时也必须展示“配置—编辑—检查”三条可观察轨道；空状态只说明下一步，不能替代真实的工作区结构。
- 状态轨迹线由细规则、节点和短标签组成，应用于请求生命周期、Provider 连接、隐私边界、审计历史和模型对比；它是系统的主视觉识别元素。
- 空状态必须展示下一项可执行动作，并保留未来响应、指标、JSON 与诊断将进入的结构槽位。
- 面板应表现为互相连接的仪器托盘，依靠标题带、内部读数区和功能分隔，而非悬浮式 SaaS 卡片。
- 状态轨迹线是主品牌图形，应组织 Provider 就绪、请求生命周期、隐私边界、审计历史与比较过程。
- 非 UI 图形只采用与请求/响应边界有关的技术示意与信号轨迹，不使用营销式装饰插图。
- 面板必须读取为共享规则、标题带、分隔线和读数区域连接的仪器托盘；仅模态和临时浮层保留独立圆角表面。
- 所有空状态保留其未来操作结构，例如轨迹、槽位、列、指标或诊断读数，而非只提供居中提示。
- 开口括号/脉冲点与状态轨迹线是主品牌图形，应用于侧轨激活状态、Provider 就绪、隐私边界、审计和生命周期反馈。
- 执行、确认、选择与就绪控件统一使用 Precision Teal `#0F766E`，不保留默认浏览器蓝或泛化 SaaS 强调色。
- Compare 必须读取为一个由规则、标题带、读数区与轨迹线连接的仪器控制台；页面引言保持任务导向并立即回到工作区。
- Diagnostics 使用连续仪器托盘与等宽读数呈现摘要和健康状态；任何空诊断区域也必须保留 source、cause、evidence、action、raw details 与 lifecycle 的可见检查槽位。
- 所有持久工作区表面优先表现为共享边框、标题带、内部隔断与读数槽位连接的仪器托盘；独立圆角卡片只用于临时浮层与小型控件。
- 开口括号/脉冲节点与状态轨迹线必须贯穿活动导航、Provider 就绪、隐私边界、请求生命周期、诊断和模型比较，作为唯一可识别的主图形语言。
- 空状态继续保留其未来数据的结构轮廓，例如指标、生命周期节点、列、槽位或原始详情托盘，不退化为单一的图标、提示与按钮组合。
